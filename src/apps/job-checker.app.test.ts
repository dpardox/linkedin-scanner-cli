import { describe, expect, test } from 'vitest';
import { JobCheckerApp } from './job-checker.app';

describe('JobChecker', () => {
  //

  test('to be true', () => {
    expect(true).toBe(true);
  });
});

// TODO (dpardo): complete the test
